// FILE: android/app/build.gradle
// FINAL CORRECTED VERSION

// 1. WE ADDED THE PLUGIN ID HERE
plugins {
    id 'com.android.application'
    id 'com.google.gms.google-services' // 👈 ADDED THIS LINE
}

android {
    namespace "live.qtalent.app"
    compileSdk rootProject.ext.compileSdkVersion
    defaultConfig {
        applicationId "live.qtalent.app"
        minSdkVersion rootProject.ext.minSdkVersion
        targetSdkVersion rootProject.ext.targetSdkVersion
        versionCode 1
        versionName "1.0"
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"
     
   aaptOptions { [cite: 2]
             // Files and dirs to omit from the packaged assets dir, modified to accommodate modern web apps. [cite: 2]
             // Default: https://android.googlesource.com/platform/frameworks/base/+/282e181b58cf72b6ca770dc7ca5f91f135444502/tools/aapt/AaptAssets.cpp#61 [cite: 3]
            ignoreAssetsPattern '!.svn:!.git:!.ds_store:!*.scc:.*:!CVS:!thumbs.db:!picasa.ini:!*~'
        }
    }
    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }
}

repositories {
    flatDir{
        dirs '../capacitor-cordova-android-plugins/src/main/libs', 'libs'
    }
}

dependencies {
    implementation fileTree(include: ['*.jar'], dir: 'libs')
    implementation "androidx.appcompat:appcompat:$androidxAppCompatVersion"
    implementation "androidx.coordinatorlayout:coordinatorlayout:$androidxCoordinatorLayoutVersion"
    implementation "androidx.core:core-splashscreen:$coreSplashScreenVersion"
    implementation project(':capacitor-android')
    testImplementation "junit:junit:$junitVersion"
    androidTestImplementation "androidx.test.ext:junit:$androidxJunitVersion"
    androidTestImplementation "androidx.test.espresso:espresso-core:$androidxEspressoCoreVersion"
    implementation project(':capacitor-cordova-android-plugins')

    // 2. WE ADDED THE FIREBASE MESSAGING LIBRARY ("THE MAILBOX")
    implementation 'com.google.firebase:firebase-messaging:23.4.1' // 👈 ADDED THIS LINE
}

apply from: 'capacitor.build.gradle'

// 3. WE DELETED THE OLD, UNRELIABLE "try...catch" BLOCK FROM HERE
// 
// ▼▼▼ FIX: Removed the extra '}' that was here ▼▼▼
//